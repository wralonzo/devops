version: '3'
services:
  app:
    build: .
    command: sh -c "npm install && ng serve --host 0.0.0.0 --port 42389"
    working_dir: /home/node/app
    volumes:
      - ./:/home/node/app
      - node_modules:/home/node/app/node_modules
volumes:
  node_modules: